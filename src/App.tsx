import type { FC } from 'react';

import MainPage from './pages/MainPage';

import './App.css';

const App: FC = () => (
  <div className="App">
    <header className="App-header">Map</header>
    <MainPage />
  </div>
);
export default App;
